# Workspace .npmrc configuration
# Used for development and workspace builds only
# For production builds, see .npmrc.prod
# ⚠️  DO NOT add secrets here - use .npmrc.secrets instead

# node-linker=hoisted
link-workspace-packages=true
